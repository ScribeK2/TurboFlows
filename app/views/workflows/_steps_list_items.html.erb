<% ordinals = step_ordinals(workflow) %>
<%# Rendered inside the #steps-list container after a grow, a delete, a
    template apply or a connections change replaces the list %>
<% steps.each do |step| %>
  <%= render "workflows/step_row", step: step, workflow: workflow, ordinals: ordinals %>
<% end %>
