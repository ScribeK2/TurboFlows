<%# Message-specific fields partial %>
<%# Message steps display read-only information/guidance to the CSR %>

<div class="field-container">
  <div class="alert alert--info flex items-start gap-3 mb-4">
    <svg class="icon icon--sm flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
    </svg>
    <div class="flex-1">
      <p class="text-sm">
        <strong>Message Step:</strong> Display information or guidance to the CSR. This step auto-advances and doesn't require user input. Use <code>{{variable_name}}</code> to include dynamic content.
      </p>
    </div>
  </div>

  <%# Content Field %>
  <div class="form-group">
    <label class="form-label">
      Message Content
      <span class="font-normal">(optional)</span>
    </label>
    <%= form_with model: [workflow, step], url: workflow_step_path(workflow, step), method: :patch, local: true do |f| %>
      <%= f.rich_text_area :content,
          data: { step_form_target: "field", variable_autocomplete_target: "input" } %>
    <% end %>
  </div>

  <%# Can Resolve Checkbox %>
  <div class="form-group" data-controller="checkbox-hidden">
    <label class="flex items-center gap-2 cursor-pointer">
      <input type="hidden" name="workflow[steps][][can_resolve]"
             value="<%= step['can_resolve'] == true || step['can_resolve'] == 'true' ? 'true' : 'false' %>"
             data-checkbox-hidden-target="hidden"
             data-step-form-target="field">
      <input type="checkbox"
             data-checkbox-hidden-target="checkbox"
             data-action="change->checkbox-hidden#toggle"
             class="form-checkbox">
      <span class="text-sm">This step may resolve the issue</span>
    </label>
    <p class="form-hint mt-1 ml-6">
      When enabled, the scenario player shows a "This resolved the issue" button alongside Continue.
    </p>
  </div>
</div>
