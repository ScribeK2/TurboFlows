<!DOCTYPE html>
<html data-controller="dark-mode">
  <head>
    <title>TurboFlows</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="view-transition" content="same-origin">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Initialize theme before page renders to prevent flash -->
    <script nonce="<%= content_security_policy_nonce %>">
      (function() {
        const savedTheme = localStorage.getItem("theme");
        const prefersDark = window.matchMedia("(prefers-color-scheme: dark)").matches;
        document.documentElement.dataset.theme =
          (savedTheme === "dark" || (!savedTheme && prefersDark)) ? "dark" : "light";
      })();
    </script>

    <!-- Favicon -->
    <link rel="icon" href="/favicon.ico" sizes="any">
    <link rel="icon" href="/favicon.svg" type="image/svg+xml">

    <%= stylesheet_link_tag "application",
          "_global", "reset", "base",
          "buttons", "forms", "cards", "tables", "badges", "dialogs",
          "flash", "dropdowns", "pagination", "icons",
          "layout", "navigation", "dashboard", "workflows", "editor",
          "steps", "scenarios", "admin", "auth",
          "animations", "utilities", "print",
          "data-turbo-track": "reload", preload_links_header: false %>

    <!-- Importmap for ES modules -->
    <%= javascript_importmap_tags %>
  </head>

  <body class="page-body">
    <!-- Centered Auth Content -->
    <div class="auth-page">
      <%= render "shared/flash_messages" %>

      <!-- Brand Header -->
      <div class="auth-brand">
        <%= render "shared/brand_icon", css_class: "auth-brand__icon" %>
        <span class="auth-brand__text">TurboFlows</span>
      </div>

      <%= yield %>
    </div>
  </body>
</html>
